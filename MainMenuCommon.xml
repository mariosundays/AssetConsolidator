<?xml version="1.0" encoding="UTF-8"?>
<mainMenu>
  <!-- Same "Tools" menu id as flipbook_to_mp4, so Houdini merges the two
       entries into one menu instead of creating a duplicate. -->
  <menuBar>
    <subMenu id="mdomingos_tools_menu">
      <label>Tools</label>
      <insertBefore>help_menu</insertBefore>
    </subMenu>
  </menuBar>

  <menu>
    <addScriptItem id="asset_consolidator">
      <parent>mdomingos_tools_menu</parent>
      <label>Consolidate Assets</label>
      <scriptCode>
        <![CDATA[
import asset_consolidator
try:
    from importlib import reload
    reload(asset_consolidator)
except Exception:
    pass
asset_consolidator.main(kwargs if "kwargs" in dir() else None)
]]>
      </scriptCode>
    </addScriptItem>
  </menu>
</mainMenu>
